apiVersion: v1
kind: Pod
metadata:
  name: flask-pod
  labels:
    app: flask-helloworld
spec:
  containers:
  - name: flask
    image: jamonation/flask-helloworld:v0
    ports:
    - containerPort: 5000
